import type { Metadata } from 'next';
export const metadata: Metadata = { title: '球會', description: '瀏覽香港各區網球會，加入成為會員' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
