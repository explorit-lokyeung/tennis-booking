import type { Metadata } from 'next';
export const metadata: Metadata = { title: '課程', description: '搜尋香港網球課程，初級至高級' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
